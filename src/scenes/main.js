import {
    HEIGHT,
    WIDTH,
} from '../config';

function preload() {
    const { game } = this;
}

function create() {
    const { game } = this;
}

function update() {
    const { game } = this;
}

export default {
    preload,
    create,
    update,
};

