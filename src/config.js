export const HEIGHT = 600;
export const WIDTH = 900;

const config = {
    height: HEIGHT,
    width: WIDTH,
    renderer: Phaser.CANVAS,
    parent: '',
    transparent: false,
    antialias: false,
    physicsConfig: {
        arcade: true,
    },
};

export default config;
